quick start:

```
# install vagrant-puppet-install for vagrant
$ vagrant plugin install vagrant-puppet-install
# optionally also install to speed things up
$ vagrant plugin install vagrant-cachier
$ git clone this repo
$ vagrant up
```
